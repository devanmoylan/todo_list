<li class="<%= "completed" if item.completed? %>">
  <div class="view">

    <%= form_for ([@list, item]) do |f| %>
        <%= f.check_box :status, :class => "toggle", :checked => (true if item.completed?) %>
    <% end %>

    <label><%= item.name %></label>
  </div>
</li>
